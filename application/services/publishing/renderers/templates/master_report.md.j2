# Strategic Modernization Report: {{ model.system_context.project_name }}

## 1. Executive Command Center

**Global System Meta-Data**
- **Scale:** {{ model.system_context.total_files }} Files | {{ model.system_context.total_classes }} Classes
- **Architecture/Era:** {{ model.system_context.framework }} ({{ model.system_context.php_era }})
- **Modernization Readiness:** {{ model.system_context.overall_readiness }}%
- **Test Coverage:** {{ model.system_context.test_coverage }}
- **Lines of Code:** {{ model.system_context.lines_of_code }}
- **Connectivity:** {{ model.system_context.connectivity }} Edges
- **Avg Complexity (WMC):** {{ model.system_context.avg_complexity }}

**Architecture Modernization Scorecard**
{% if model.legacy_intelligence and model.legacy_intelligence.score_dimensions %}
{% for key, val in model.legacy_intelligence.score_dimensions.items() %}
- **{{ key }}:** {% if val is number %}{{ (val * 10) | round(1) }}/10{% else %}{{ val }}{% endif %}
{%- endfor %}
{% else %}
- *Scorecard Data Unavailable*
{% endif %}

**Architectural Footprint**
{% for key, val in model.system_context.architectural_footprint.items() -%}
- {{ key }}: {{ val }}
{% endfor %}

**AI Executive Assessment**
*Current State:* {{ model.ai_executive_summary.get('current_state', '[Not Available]') }}

**Critical Risks:** {{ model.ai_executive_summary.get('critical_risks', '[Not Available]') }}

*Strategic Roadmap:*
{% for step in model.ai_executive_summary.get('strategic_roadmap', []) %}
**Step {{ step.step_number }}: {{ step.title }}**
- *Description:* {{ step.description }}
- *Rationale:* {{ step.rationale }}
{% endfor %}

## 2. Boundary Intelligence & External Surface

{% if model.ai_executive_summary.get('boundary_layer_insights') %}
{{ model.ai_executive_summary.get('boundary_layer_insights') }}
{% endif %}

{% if model.boundary_intelligence %}
### Presentation Layer Coupling
- **Global UI Entanglement:** {{ model.boundary_intelligence.kpis.get('Global UI Entanglement', '0%') }}
- **Fat Views (DB Coupled):** {{ model.boundary_intelligence.kpis.get('Fat Views (DB-Coupled UI)', 0) }}

<details>
<summary>View Fat Views & Entanglement Data</summary>

| File | Entanglement Ratio | HTML/Echo Nodes | DB Operations | Severity |
| :--- | :--- | :--- | :--- | :--- |
{% for p in model.boundary_intelligence.presentation_coupling -%}
| <kbd>{{ p.file_path }}</kbd> | {{ p.ui_entanglement_ratio }}% | {{ p.html_echo_nodes }} | {{ p.db_queries }} | {{ p.severity }} |
{% endfor %}

</details>

### API & Endpoint Surface
- **Total Endpoints:** {{ model.boundary_intelligence.api_surface | length }}

<details>
<summary>View Discovered Endpoints</summary>

| Entry Point | Classification |
| :--- | :--- |
{% for a in model.boundary_intelligence.api_surface -%}
| <kbd>{{ a.path }}</kbd> | {{ a.type }} |
{% endfor %}

</details>

### Vendor & Shadow IT Intelligence
- **Vendor Files Scanned:** {{ model.boundary_intelligence.vendor_inventory | length }}

<details>
<summary>View Shadow IT Inventory</summary>

| File | Vendor Type | Status |
| :--- | :--- | :--- |
{% for v in model.boundary_intelligence.vendor_inventory -%}
| <kbd>{{ v.file_path }}</kbd> | {{ v.vendor_type }} | {{ v.status }} |
{% endfor %}

</details>

{% else %}
*Boundary Intelligence data is unavailable.*
{% endif %}

## 3. Layered Architecture & Topology

{% if model.ai_executive_summary.get('architecture_insights') %}
{{ model.ai_executive_summary.get('architecture_insights') }}
{% endif %}

{% if model.layered_architecture %}
- **Presentation vs. Logic Ratio:** {{ model.layered_architecture.presentation_ratio }}% of classified files are UI/Routing related.

<details>
<summary>View Project Folder Structure</summary>

```text
{{ model.layered_architecture.directory_tree.ascii }}
```

</details>

### Bounded Contexts

#### Top 5 Bounded Contexts by File Count
```mermaid
pie title Files per Context
{% for c in model.layered_architecture.bounded_contexts[:5] -%}
    "{{ c.name }}" : {{ c.file_count }}
{% endfor %}
```

<details>
<summary>View Complete Context Coupling Data</summary>

| Domain Name | Files | Internal Calls | External Calls | Coupling Ratio | DB Access | Auth Access |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
{% for c in model.layered_architecture.bounded_contexts -%}
| <kbd>{{ c.name }}</kbd> | {{ c.file_count }} | {{ c.internal_calls }} | {{ c.external_calls }} | {{ c.coupling_ratio }} | {{ c.db_access }} | {{ c.auth_access }} |
{% endfor %}

</details>

*(Note: System topology graph JSON is available in the SARIF/JSON data bundle).*
{% else %}
*Layered Architecture data is unavailable.*
{% endif %}

## 4. Database Intelligence

{% if model.ai_executive_summary.get('database_coupling_insights') %}
{{ model.ai_executive_summary.get('database_coupling_insights') }}
{% endif %}

{% if model.database_intelligence %}
### CRUD Access Taxonomy

<details>
<summary>View Database Taxonomy Data</summary>

| File Path | Reads | Writes | ORM Usage |
| :--- | :--- | :--- | :--- |
{% for file, metrics in model.database_intelligence.taxonomy.items() -%}
{% if metrics.reads > 0 or metrics.writes > 0 or metrics.orm > 0 -%}
| <kbd>{{ file }}</kbd> | {{ metrics.reads }} | {{ metrics.writes }} | {{ metrics.orm }} |
{% endif -%}
{% endfor %}

</details>

### Table Ownership & Pressure

<details>
<summary>View Database Pressure Metrics</summary>

| Table Name | Primary Owner | Write Intensity | Shared Pressure |
| :--- | :--- | :--- | :--- |
{% for db in model.database_intelligence.table_ownership -%}
| <kbd>{{ db.table }}</kbd> | <kbd>{{ db.primary_owner }}</kbd> | - | - |
{% endfor -%}
{% for db in model.database_intelligence -%}
{% if db is mapping -%}
| <kbd>{{ db.table_name }}</kbd> | - | {{ db.write_intensity }} | {{ db.shared_table_pressure }} |
{% endif -%}
{% endfor %}

</details>
{% else %}
*No database intelligence data found.*
{% endif %}

## 5. Runtime & Global State Intelligence

{% if model.ai_executive_summary.get('global_state_insights') %}
{{ model.ai_executive_summary.get('global_state_insights') }}
{% endif %}

{% if model.global_state_intelligence %}
### Superglobal Usage Distribution

{% set total_globals = model.global_state_intelligence.superglobal_totals.values() | sum %}
{% if total_globals > 0 %}
```mermaid
pie title Superglobal Calls
{% for var, count in model.global_state_intelligence.superglobal_totals.items() -%}
{% if count > 0 -%}
    "{{ var }}" : {{ count }}
{% endif -%}
{% endfor %}
```

<details>
<summary>View Raw Variables List</summary>

| Superglobal | Occurrences |
| :--- | :--- |
{% for var, count in model.global_state_intelligence.superglobal_totals.items() -%}
{% if count > 0 -%}
| <kbd>{{ var }}</kbd> | {{ count }} |
{% endif -%}
{% endfor %}

</details>
{% else %}
> **No superglobal accesses detected.**
{% endif %}

### Side-Effect Breakdown

{% set total_effects = model.global_state_intelligence.side_effect_totals.values() | sum %}
{% if total_effects > 0 %}
<details>
<summary>View Side-Effect Totals</summary>

| Effect Type | Count |
| :--- | :--- |
{% for effect, count in model.global_state_intelligence.side_effect_totals.items() -%}
{% if count > 0 -%}
| <kbd>{{ effect }}</kbd> | {{ count }} |
{% endif -%}
{% endfor %}

</details>
{% else %}
> **No direct state mutations detected.**
{% endif %}
{% else %}
*No global state intelligence data found.*
{% endif %}

## 6. Legacy PHP Intelligence

{% if model.legacy_intelligence %}

### Era Classification
- **Classified Era:** {{ model.legacy_intelligence.classified_era }}
- **Procedural Ratio:** {{ "%.1f"|format(model.legacy_intelligence.procedural_ratio * 100) }}%
- **Namespace Coverage:** {{ "%.1f"|format(model.legacy_intelligence.namespace_ratio * 100) }}%

#### Era Signals Detected
{% if model.legacy_intelligence.era_signals %}
| Severity | Estimated Era | Occurrences | Detail |
| :--- | :--- | :--- | :--- |
{% for signal in model.legacy_intelligence.era_signals -%}
| <kbd>{{ signal.severity }}</kbd> | {{ signal.era }} | {{ signal.count }} | {{ signal.detail }} |
{% endfor %}
{% else %}
*No legacy era signals detected. The codebase appears to use modern PHP patterns.*
{% endif %}

### Structural Composition
| Category | Count |
| :--- | :--- |
| **Total Files Scanned** | {{ model.legacy_intelligence.total_files_scanned }} |
| **OOP Files (has classes)** | {{ model.legacy_intelligence.files_with_classes }} |
| **Namespace-aware Files** | {{ model.legacy_intelligence.files_namespace_aware }} |
| **Procedural-only Files** | {{ model.legacy_intelligence.files_procedural_only }} |
| **Variable Variables ($$var)** | {{ model.legacy_intelligence.variable_variable_count }} |
| **Hosting Assumption Calls** | {{ model.legacy_intelligence.hosting_signal_count }} |

### Legacy Anti-Pattern Inventory
{% if model.legacy_intelligence.pattern_totals %}
| Pattern | Occurrences |
| :--- | :--- |
{% for pattern, count in model.legacy_intelligence.pattern_totals.items() -%}
{% if count > 0 -%}
| <kbd>{{ pattern }}</kbd> | {{ count }} |
{% endif -%}
{% endfor %}
{% else %}
*No legacy anti-patterns found.*
{% endif %}

{% else %}
*Legacy Intelligence data is unavailable.*
{% endif %}

## 7. Strategic Advisory

{% if model.strategic_advisory %}
### Modernization ROI Matrix
| Module Context | Recommended Strategy | ROI Score | Migration Effort | Primary Blocker |
| :--- | :--- | :--- | :--- | :--- |
{% for rec in model.strategic_advisory.get('recommendations', []) -%}
| <kbd>{{ rec.Context }}</kbd> | **{{ rec['Recommended Strategy'] }}** | {{ rec['Modernization ROI'] }}% | {{ rec['Migration Effort'] }} | {{ rec['Primary Blocker'] }} |
{% endfor %}
{% else %}
*Strategic advisory data is unavailable.*
{% endif %}

## 8. Security Posture
{{ model.ai_executive_summary.get('security_posture', 'No security posture data available.') }}

## 9. Testing Strategy
{{ model.ai_executive_summary.get('testing_strategy', 'No testing strategy available.') }}

## 10. Quick Wins
{% if model.ai_executive_summary.get('quick_wins') %}
{% for qw in model.ai_executive_summary.get('quick_wins', []) %}
- **{{ qw.title }}**: {{ qw.impact }}
{% endfor %}
{% else %}
*No quick wins identified.*
{% endif %}

## 11. Security & Risk Audit

{{ model.ai_executive_summary.get('security_posture', '*No executive security posture analysis available.*') }}

{% if model.file_matrix and model.file_matrix | length > 0 %}
### The Risk Matrix (Top 100 Highest Risk Files)
*Every file is scored against the Maintainability Index and Cyclomatic Complexity standards. To prevent report bloat, this matrix is capped at the 100 most structurally dangerous components. (For the complete inventory, refer to the CSV export).*

| File Path | Overall Risk | MI (0-100) | Cyclomatic Complexity | Nesting | Method LOC | Fan-Out | Security Sinks | Global Accesses |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
{% for f in model.file_matrix -%}
{% set risk = f.get('Overall Risk', f.get('Overall File Risk', 'LOW')) -%}
{% if risk == 'CRITICAL' %}{% set risk_emoji = '🔴 CRITICAL' -%}
{% elif risk == 'HIGH' %}{% set risk_emoji = '🟠 HIGH' -%}
{% elif risk == 'MEDIUM' %}{% set risk_emoji = '🟡 MEDIUM' -%}
{% else %}{% set risk_emoji = '🟢 LOW' %}{% endif -%}
| <kbd>{{ f.get('File Name', 'Unknown') }}</kbd> | {{ risk_emoji }} | {{ f.get('Maintainability Index', 0) }} | {{ f.get('Cyclomatic Complexity', 0) }} | {{ f.get('Max Nesting Depth', 0) }} | {{ f.get('Max Method LOC', 0) }} | {{ f.get('Fan-Out', 0) }} | {{ f.get('Security Sinks', 0) }} | {{ f.get('Global Accesses', 0) }} |
{% endfor %}
{% else %}
*No file-level risk matrix available.*
{% endif %}

### Critical Incident Registry
{% if model.findings and model.findings | length > 0 %}
{% for f in model.findings -%}
#### [{{ f.priority }}] {{ f.observation }}
- **Business Impact:** {{ f.impact }}
- **Strategic Action:** {{ f.recommended_action }} *(Confidence: {{ f.confidence }})*
- **Evidence/Metrics:**
{% for ev in f.evidence -%}
  - `{{ ev.type }}`: <kbd>{{ ev.target }}</kbd> {% if ev.metric_value %}({{ ev.metric_value }}){% endif %}
{% endfor %}

---
{% endfor %}
{% else %}
*No critical risks identified.*
{% endif %}
